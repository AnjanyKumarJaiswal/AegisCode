export default function Subheadline() {
    return (
        <p className="subheadline">
            AegisCode runs two LLM agents in parallel against every AI-generated
            commit — detecting injection flaws, hardcoded secrets, and insecure
            deserialization in real time, then iterates patches until the risk score
            clears threshold.
        </p>
    );
}
